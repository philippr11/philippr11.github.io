"use client";

import * as React from "react"
import Image from "next/image";

const Logo = (() => (
  <div className="h-16">
    <img src="./hd_transparent_logo.png" alt="Finanzferkel" style={{width: '100%', height: '100%'}} className="object-contain" />
  </div>
));

export default Logo